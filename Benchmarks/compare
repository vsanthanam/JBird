#!/bin/bash

rm Package.resolved
swift package reset

export BENCHMARK_TARGET=jbird

export JBIRD_SOURCE=release

swift package --allow-writing-to-package-directory benchmark baseline update release

rm Package.resolved
swift package reset

export JBIRD_SOURCE=local

swift package --allow-writing-to-package-directory benchmark baseline update main

swift package benchmark baseline compare release main > results.txt