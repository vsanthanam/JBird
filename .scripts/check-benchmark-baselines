#!/bin/bash

cd Benchmarks

BENCHMARK_TARGET="remote" BENCHMARK_DISABLE_JEMALLOC=true swift package -Xswiftc -DUSE_REMOTE --allow-writing-to-package-directory benchmark baseline update remote

BENCHMARK_TARGET="local" BENCHMARK_DISABLE_JEMALLOC=true swift package benchmark baseline check remote